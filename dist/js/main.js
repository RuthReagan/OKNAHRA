// OKNAHRA site — minimal progressive-enhancement JS (no framework needed).
document.addEventListener('DOMContentLoaded', function () {
  // Nothing required for first load; nav toggle is handled inline in the
  // header markup so the menu still works if this file fails to load.
});
